# HousingFixturePointFrameAPI

> Generated from the WoW: Forever client (build 69913, interface 16001) on 2026-09-20 09:09:13.

**5** functions · **0** events · **1** types

## Functions

### HousingFixturePointFrameAPI.HasAttachedFixture

```lua
hasAttachedFixture = HasAttachedFixture()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `hasAttachedFixture` | bool | no |  |

**Example**

```lua
local hasAttachedFixture = HasAttachedFixture()
```

### HousingFixturePointFrameAPI.IsSelected

```lua
isSelected = IsSelected()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `isSelected` | bool | no |  |

**Example**

```lua
local isSelected = IsSelected()
```

### HousingFixturePointFrameAPI.IsValid

```lua
isValid = IsValid()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `isValid` | bool | no |  |

**Example**

```lua
local isValid = IsValid()
```

### HousingFixturePointFrameAPI.Select

```lua
Select()
```

**Example**

```lua
Select()
```

### HousingFixturePointFrameAPI.SetUpdateCallback

```lua
SetUpdateCallback(cb)
```

**Arguments**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `cb` | FixturePointUpdatedCallback | no |  |

**Example**

```lua
SetUpdateCallback(cb)
```

## Types

### FixturePointUpdatedCallback (CallbackType)

