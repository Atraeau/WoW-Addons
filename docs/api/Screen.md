# Screen

> Generated from the WoW: Forever client (build 69913, interface 16001) on 2026-09-20 09:09:13.

**5** functions · **0** events · **0** types

## Functions

### Screen.GetDefaultScale

```lua
scale = GetDefaultScale()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `scale` | number | no |  |

**Example**

```lua
local scale = GetDefaultScale()
```

### Screen.GetPhysicalScreenSize

```lua
sizeX, sizeY = GetPhysicalScreenSize()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `sizeX` | number | no |  |
| `sizeY` | number | no |  |

**Example**

```lua
local sizeX, sizeY = GetPhysicalScreenSize()
```

### Screen.GetScreenDPIScale

```lua
scale = GetScreenDPIScale()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `scale` | number | no |  |

**Example**

```lua
local scale = GetScreenDPIScale()
```

### Screen.GetScreenHeight

```lua
height = GetScreenHeight()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `height` | number | no |  |

**Example**

```lua
local height = GetScreenHeight()
```

### Screen.GetScreenWidth

```lua
width = GetScreenWidth()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `width` | number | no |  |

**Example**

```lua
local width = GetScreenWidth()
```

