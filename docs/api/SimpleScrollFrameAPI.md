# SimpleScrollFrameAPI

> Generated from the WoW: Forever client (build 69913, interface 16001) on 2026-09-20 09:09:13.

**9** functions · **0** events · **0** types

## Functions

### SimpleScrollFrameAPI.GetHorizontalScroll

```lua
offset = GetHorizontalScroll()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `offset` | uiUnit | no |  |

**Example**

```lua
local offset = GetHorizontalScroll()
```

### SimpleScrollFrameAPI.GetHorizontalScrollRange

```lua
range = GetHorizontalScrollRange()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `range` | uiUnit | no |  |

**Example**

```lua
local range = GetHorizontalScrollRange()
```

### SimpleScrollFrameAPI.GetScrollChild

```lua
scrollChild = GetScrollChild()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `scrollChild` | SimpleFrame | no |  |

**Example**

```lua
local scrollChild = GetScrollChild()
```

### SimpleScrollFrameAPI.GetVerticalScroll

```lua
offset = GetVerticalScroll()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `offset` | uiUnit | no |  |

**Example**

```lua
local offset = GetVerticalScroll()
```

### SimpleScrollFrameAPI.GetVerticalScrollRange

```lua
range = GetVerticalScrollRange()
```

**Returns**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `range` | uiUnit | no |  |

**Example**

```lua
local range = GetVerticalScrollRange()
```

### SimpleScrollFrameAPI.SetHorizontalScroll

```lua
SetHorizontalScroll(offset)
```

**Arguments**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `offset` | uiUnit | no |  |

**Example**

```lua
SetHorizontalScroll(offset)
```

### SimpleScrollFrameAPI.SetScrollChild

```lua
SetScrollChild(scrollChild)
```

**Arguments**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `scrollChild` | SimpleFrame | no |  |

**Example**

```lua
SetScrollChild(scrollChild)
```

### SimpleScrollFrameAPI.SetVerticalScroll

```lua
SetVerticalScroll(offset)
```

**Arguments**

| Name | Type | Nilable | Description |
|------|------|---------|-------------|
| `offset` | uiUnit | no |  |

**Example**

```lua
SetVerticalScroll(offset)
```

### SimpleScrollFrameAPI.UpdateScrollChildRect

```lua
UpdateScrollChildRect()
```

**Example**

```lua
UpdateScrollChildRect()
```

